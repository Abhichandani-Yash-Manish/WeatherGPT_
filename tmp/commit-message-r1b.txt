Design language and decisions recorded, and the R1 shell gains its weather ground and topbar

Decisions: all six from docs/87 confirmed green (layout chat-centre + workbench panel, assistant-ui primitives
with the Radix fallback, staged contract with a streaming seam, self-hosted fonts, Tailwind+shadcn/Radix) and the
CSP one moot because R0 measured that no relaxation is needed. docs/88 records them together with the new scope
from this message: uplift the surfaces the first run did not focus on, then a landing page and an owner gate,
under a weather-native design language.

Design language (docs/88): six sky phases selected from the local clock - or a sourced sunrise/sunset when a
caller has one - applied as data-sky with contrast-checked surface and ink pairs in both themes; the storm phase
is reserved for a surface that already states an official warning, so decoration can never imply one; the band is
aria-hidden, carries the note that it is decoration rather than a condition, and respects reduced motion;
Meteocons (MIT) may render a condition only beside evidence, Lucide (ISC) carries the chrome; the warning ramp
stays reserved; values use a tabular face so a number never looks like chrome.

Delivered in the shell: src/shell/sky.ts + styles/sky.css (phase logic with a sourced sun time, and the reserved
storm phase), SkyBackground, and the topbar on TanStack Query - store state from /api/health with an unreachable
read shown as unreachable, the language select from /api/languages with unmeasured languages marked, a theme
cycle and a new-conversation action. Thirteen frontend checks pass (rail, routing contract, shortcuts, deep link,
topbar states, language options, sky phases); the initial graph is 83 KB gzip against the 312 KB budget.

Browser acceptance for the React shell is NOT captured this round: headless Chrome hung after the earlier
successful runs and the CDP client reported a socket error, so the attempt is recorded rather than replaced by a
claim (research/reviews/frontend-react-r1-20260917/r1-verification.json). It stays an open R1 item, together with
porting the 110 vanilla component checks one-for-one, which gates R2. 28 verification steps and 13 frontend
checks pass; the vanilla frontend remains the default.