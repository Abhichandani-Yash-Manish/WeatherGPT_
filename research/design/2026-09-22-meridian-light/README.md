# Meridian Light exploration

The app in this research directory is the **earlier independent interaction prototype**. Its local replay is not connected to WeatherGPT's engine.

The selected direction is now implemented in the active frontend at `frontend/src/gpt/` and `frontend/src/chat/`, served by the main workspace at http://127.0.0.1:8765. Use that app for review. See `docs/160-meridian-react-ui.md` and this directory's `design-qa.md` for scope, evidence and limitations.

The prototype remains as design history. Its unfinished local replay/stop behavior is not shipped in the main React app.
